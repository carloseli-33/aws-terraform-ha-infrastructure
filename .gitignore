# Terraform local state (NEVER commit this — it contains secrets)
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Sensitive variable files
*.tfvars
!example.tfvars

# macOS
.DS_Store

# Editor
.vscode/settings.json
*.swp
